# Public Expo variables must use the EXPO_PUBLIC_ prefix.
# Keep environment-specific values in local .env files or your secure CI environment.
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_ENVIRONMENT=development
